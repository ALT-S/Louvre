<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Blog - Billet simple pour l'Alaska{% endblock %}</title>

    {% block stylesheets %}
        {# On charge le CSS de bootstrap depuis le site directement #}
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="{{ asset('bundles/altapp/css/billetterie.css') }}">
    {% endblock %}

</head>

<body>

<div class="container">

    <div class="row">

        <div class="col-sm-3 sidebar">
            <img alt="logo louvre" class="logo" src= {{ asset('bundles/altapp/image/images.jpeg') }}>
            <h1>Musée du Louvre</h1>

            <div class="menu">
                <h4>Menu</h4>
                <ul class="nav nav-pills nav-stacked">
                    <li>
                        <a class="{% if app.request.attributes.get('_route') == 'alt_app_accueil' %}navbar-brand{% endif %} "
                           href="{{ path('alt_app_accueil') }}">Accueil <i class=" glyphicon glyphicon-home"></i> </a>
                    </li>

                    <li>
                        <a class="{% if app.request.attributes.get('_route') == 'alt_app_tarifs' %}navbar-brand{% endif %}"
                           href="{{ path('alt_app_tarifs') }}">Tarifs</a>
                    </li>

                    <li>
                        <a class="{% if app.request.attributes.get('_route') == 'alt_app_aide' %}navbar-brand{% endif %}"
                           href="{{ path('alt_app_aide') }}">Aide/FAQ</a>
                    </li>

                    <li>
                        <a class="{% if app.request.attributes.get('_route') == 'alt_app_cgv' %}navbar-brand{% endif %}"
                           href="{{ path('alt_app_cgv') }}">CGV</a>
                    </li>

                </ul>
            </div>

            <div class="pannier">
                <h4>Mon pannier</h4>

                <div class="resa">
                    <p>Votre panier est vide.</p>
                </div>

            </div>
        </div><!-- /.sidebar -->

        <div class="col-sm-9 accueil">

            <h1>"Billetterie officielle"</h1>

            <div class="louvre">
                {% if app.session.flashBag.has('notice') %}
                    <div class="alert alert-success">
                        {% for msg in app.session.flashBag.get('notice') %}
                            {{ msg }}
                        {% endfor %}
                    </div>
                {% endif %}

                {% block body %}

                {% endblock %}
            </div><!-- /.louvre -->
        </div><!-- /.accueil -->
    </div><!-- /.row -->

    <footer>
        <hr>
        <div class="auteur " style="text-align: center">
            <p>
                <a href=""><i class=" glyphicon glyphicon-user"></i></a>
            </p>
        </div>
    </footer>

</div><!-- /.container -->

{% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
{% endblock %}

</body>
</html>