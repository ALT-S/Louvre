{% extends "::layout.html.twig" %}

{% block body %}
    <h3>4. Renseignez les informations</h3>
    <div class="row">
        <div class="col-sm-3">
            BILLET N°
        </div>

        <div class="col-sm-9 ">
            {{ form_start(form, {'method': 'POST'}) }}

            {{ form_row(form.nom) }}

            {{ form_row(form.prenom) }}

            {{ form_row(form.pays) }}

            {{ form_row(form.dateNaissance) }}

            {{ form_row(form.tarifReduit) }}

            {{ form_widget(form.enregistrer, {'attr': {'class': 'btn btn-primary'}}) }}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}